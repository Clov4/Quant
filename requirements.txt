# Core data / numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyarrow>=12.0
SQLAlchemy>=2.0

# Data fetching / scraping (Casablanca Bourse provider)
requests>=2.28
beautifulsoup4>=4.11
lxml>=4.9
PyYAML>=6.0

# GUI / charting
streamlit>=1.30
plotly>=5.15

# Tests
pytest>=7.0

# --- Optional ---
# tvdatafeed (unofficial TradingView feed) — only needed if you enable the TV provider.
#   pip install --upgrade --no-cache-dir git+https://github.com/rongardF/tvdatafeed.git
# APScheduler — only needed for the long-running scheduler in csequant/live/scheduler.py
#   pip install APScheduler
